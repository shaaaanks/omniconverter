<resources>
    <!-- LORA Font Family -->
    <attr name="fontFamily" format="string" />

    <!-- Base application theme. -->
    <style name="Base.Theme.FYPProject" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:textViewStyle">@style/TextViewLora</item>
        <item name="buttonStyle">@style/ButtonLora</item>
    </style>

    <style name="Theme.FYPProject" parent="Base.Theme.FYPProject" />

    <!-- Splash Screen Theme -->
    <style name="Theme.Splash" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/white</item>
        <item name="android:textViewStyle">@style/TextViewLora</item>
    </style>

    <!-- LORA Font Styles -->
    <style name="TextViewLora" parent="android:Widget.TextView">
        <item name="android:fontFamily">serif</item>
        <item name="android:letterSpacing">0.01</item>
    </style>

    <style name="ButtonLora" parent="android:Widget.Button">
        <item name="android:fontFamily">serif</item>
    </style>
</resources>